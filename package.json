{
  "name": "client-subscriton_sections",
  "version": "1.1.1",
  "description": "client-site sections for subscription access and recuring's plans ",
  "main": "index.js",
  "scripts": {
    "tw:build": "npx tailwindcss -i ./styles/input.css -o ./dist/output.css --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Delvalle Services L.L.C.",
  "license": "ISC",
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.7",
    "tailwindcss": "^3.3.3"
  }
}
